<section id="users-edit">
  <h1 class="page-title"><%= t ".title" %></h1>
  <div class="actions">
    <%= link_to t(".back"), products_path, class: :btn %>
  </div>
  <%= render "form" %>
</section>

